const ROOT = '';
const DEFAULT = `${ROOT}/`;
const SIGNIN = `${ROOT}/signin`;
const SIGNUP = `${ROOT}/signup`;

const PATHS = {
	'root':ROOT,
	'default':DEFAULT,
	'signin':SIGNIN,
	'signup':SIGNUP
}
export default PATHS;