const ROOT = '';
const DEFAULT = `${ROOT}/`;
const SIGNIN = `${ROOT}/account/signin`;
const SIGNUP = `${ROOT}/account/signup`;

const PATHS = {
	'root':ROOT,
	'default':DEFAULT,
	'signin':SIGNIN,
	'signup':SIGNUP
}
export default PATHS;